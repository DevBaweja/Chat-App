// Views
import * as chatProfileView from '../views/chat-profile.view';

export const myProfile = () => {
    // Render Profile
    chatProfileView.renderMyProfile();
    // Add Event Listeners
};

// Mode - ideal, null, user
