class Navbar {
    constructor({ mode }) {
        this.mode = mode;
    }
    setMode(mode) {
        this.mode = mode;
    }
    setClassName(className) {
        this.className = className;
    }
}
export default Navbar;
