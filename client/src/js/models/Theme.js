class Theme {
    constructor(theme) {
        this.theme = theme;
    }
    changeTheme(newTheme) {
        this.theme = newTheme;
    }
}
// state.theme = new Theme('dark');
// state.theme.changeTheme('dark-light')
