class ChatPanel {
    constructor({ mode }) {
        this.mode = mode;
    }
    getUserInfo(userId) {
        return {};
    }
    setMode(mode) {
        this.mode = mode;
    }
}
export default ChatPanel;
