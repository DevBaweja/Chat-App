class ChatPanel {
    constructor({ mode }) {
        this.mode = mode;
    }
    getUserInfo(user) {
        return {};
    }
    setMode(mode) {
        this.mode = mode;
    }
}
export default ChatPanel;
