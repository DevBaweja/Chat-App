/*
const imports = ['tree', 'leaf'];
imports.forEach(item => {
    const script = document.createElement('script');
    script.src = `${attribute['']}/${item}.js`;
    document.head.append(script);
});
*/
