# Use npm registry
registry=https://registry.npmjs.org/

# Avoid legacy peer deps issues
legacy-peer-deps=true

# Use sass instead of node-sass
sass_binary_site=https://github.com/sass/node-sass/releases/download

# Skip optional dependencies that might cause issues
optional=false
